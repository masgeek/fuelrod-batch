server:
  port: 9001

spring:
  batch:
    job:
      #      names: "processMessage"
      enabled: false
    initialize-schema: ALWAYS
  main:
    web-application-type: NONE


#second, minute, hour, day of month, month, day(s) of week
batch:
  cron: "*/5 * * * * *"